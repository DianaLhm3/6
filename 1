def delete_even():

    A = list(map(int, input('Enter a list: ').split()))
    print("Original list:", A)

    result = [A[i] for i in range(len(A)) if i == 0 or i % 2 != 0]
    print("Modified list:", result)

    return result
delete_even()
